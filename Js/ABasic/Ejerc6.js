//Concatenar arreglos:
let concatenar=[1, 2, 3, 4, 5, 6];
const osos=["pardo","polar","panda"];


function concatenarArreglos(concatenar,osos) {
let arreglofinal=concatenar.concat(osos);
  console.log (arreglofinal);
}
concatenarArreglos(concatenar,osos);